import React, { SFC } from 'react'

interface ILayoutProps {}

const Layout:SFC<ILayoutProps> = () => {

    return (
        <div>
            Welcome to base React
        </div>
    )
}

export default Layout